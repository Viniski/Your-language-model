import Actions from './app-dialog-actions';
import Container from './app-dialog-container';
import Description from './app-dialog-description';
import Title from './app-dialog-title';

const AppDialog = {
  Title,
  Container,
  Description,
  Actions,
};

export default AppDialog;
